;;;; File that defines global variables that are used by multiple functions in the program

(defvar *timesteps*)            ; Variable that stores the information about the display for every timestep in the experiment
(defvar *results*)              ; Variable that stores the results from the experiments
(defvar *start-time*)           ; Variable used to calculate the time taken by the model to take each decision
(defvar *trial-time*)           ; Variable that stores the total time taken by the model to take decisions during the trial

(defvar *jameson*)              ; Variable that stores the object representing Jameson